# Runtime dependencies (pinned). Development/test tools live in requirements-dev.txt.
# Python >= 3.11.

# Web framework
Flask==3.1.3
Werkzeug==3.1.8
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52
Flask-SocketIO==5.6.1
python-socketio==5.16.4
python-engineio==4.14.0
simple-websocket==1.1.0          # WebSocket transport under async_mode='threading'
Flask-Limiter==3.12
Flask-Compress==1.24
Brotli==1.2.0
flask-swagger-ui==5.32.14
# Optional rate-limit backend; falls back to in-memory storage when no server is reachable.
redis==7.4.0

# Network operations
python-nmap==0.7.1               # also needs the `nmap` binary
ping3==4.0.4
manuf==1.1.5                     # MAC vendor lookup
dnspython==2.7.0                 # unicast mDNS probes (monitoring/mdns.py)
psutil==5.9.8

# HTTP requests (webhooks, ntfy, device probes)
requests==2.34.2

# Configuration
PyYAML==6.0.3
python-dotenv==1.2.3
