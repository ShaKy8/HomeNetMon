{
  "name": "homenetmon",
  "version": "2.4.0",
  "private": true,
  "description": "Browser (Playwright) tests for HomeNetMon; the application itself is Python (see requirements.txt).",
  "scripts": {
    "test": "playwright test TestHomeNetmon.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ShaKy8/HomeNetMon.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.55.1"
  }
}
